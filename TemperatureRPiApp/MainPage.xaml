<Page
    x:Class="TemperatureRPiApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TemperatureRPiApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            
            <TextBox x:Name="TbMessage" Margin="10" IsReadOnly="True" Width="600"></TextBox>
            <TextBox x:Name="TbTemp" Margin="10" IsReadOnly="True" ></TextBox>
            <TextBox x:Name="TbHum" Margin="10" IsReadOnly="True"/>
            <Button Margin="10" Click="ButtonBase_OnClick" Name="Btn" >Nothing</Button>

        </StackPanel>
    </Grid>
</Page>
